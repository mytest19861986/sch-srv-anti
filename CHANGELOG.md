# تاریخچه تغییرات و نسخه‌ها (Changelog)

تمامی تغییرات، اصلاحات و ویژگی‌های توسعه‌یافته در پلتفرم «سرویس یار» در این سند ثبت می‌گردد.

---

## [1.0.0] - 2026-08-28 (نسخه رسمی انتشار Pilot Ready)

### 🚀 ویژگی‌های افزوده شده (Added)
- **مدیریت جامع ۵ موجودیت مدرسه (CRUD)**: امکان افزودن، ویرایش، حذف نرم و انتساب برای دانش‌آموزان، اولیا، رانندگان، خودروها و خطوط مسیر در داشبورد مدرسه (`apps/school-web`).
- **صدور خودکار حساب و رمز عبور موقت اولیا**: ایجاد کاربر با نقش `PARENT` در AuthService و نمایش یک‌باره رمز موقت به مدیر مدرسه.
- **دسترسی فراگیر مدیر کل (Super Admin Override)**: توانایی مدیریت کامل داده‌های هر مدرسه با پارامتر `?tenantId=` و ثبت در Audit Log سراسری.
- **مودال تأیید اقدامات حساس با ثبت دلیل**: ثبت علت حذف و تغییرات بحرانی در سیستم حسابرسی به همراه شناسه اقدام‌کننده.
- **تماس تک‌لمسی راننده با اولیا**: ادغام دکمه 📞 در مانیفست راننده با استفاده از `Intent.ACTION_DIAL`.
- **ثبت عدم حضور اضطراری توسط والدین**: امکان اعلام عدم حضور فرزند در اپ والدین و بازتاب آنی روی مانیفست راننده.
- **داشبوردها و پنل‌های وب با سایدبار کامل و پشتیبانی RTL**: بیش از ۱۲ صفحه عملیاتی با لایه‌بندی بهینه فارسی و فونت Vazirmatn.
- **بسته APKهای رسمی**: تولید و انتشار بسته‌های نصبی امضاشده اپلیکیشن راننده (`ir.serviceyar.driver-v1.0.0.apk`) و اپلیکیشن والدین (`ir.serviceyar.parent-v1.0.0.apk`).
- **ویدئوی دموی رسمی ۶۰ ثانیه‌ای**: نمایش زنجیره کامل سوارشدن دانش‌آموز، همگام‌سازی آفلاین، ارسال پوش‌نوتیفیکیشن به والدین و مانیتورینگ زنده مدرسه.
- **سیستم تصویربرداری خودکار (Protocol v3)**: ثبت ۹ دسته اسکرین‌شات زنده از تمامی جریان‌های سامانه در پوشه `docs/screenshots/`.

### 🛠️ بهبودها و اصلاحات (Fixed)
- رفع کامل خطای هدرهای داخلی Fastify در بستر Bun با استفاده از چرخه استاندارد سوکت و پورت محلی.
- پشتیبانی کامل از متن‌های متصل و راست‌به‌چپ (RTL/Bidi) در موتور رندر اسکرین‌شات‌ها.
- ایزولاسیون Zero-Trust چندمستاجری (Multi-Tenancy) در تمامی کنترلرها و رد دسترسی غیرمجاز بین مدارس با کد وضعیت ۴۰۳.
- تنظیم دقیق Binding شبکه در داکر (`HOST=0.0.0.0`) و عبور ۱۰۰٪ آزمون‌های سراسری.

FROM oven/bun:1-alpine AS base
WORKDIR /app

RUN apk add --no-cache curl

COPY package.json bun.lock* ./
RUN bun install --frozen-lockfile || bun install

COPY . .

CMD ["bun", "run", "src/modules/attendance/worker.ts"]
